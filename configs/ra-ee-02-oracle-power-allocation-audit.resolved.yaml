config_role: resolved-run-follow-on
inherits_from: modqn-paper-baseline.resolved-template.yaml

track:
  phase: RA-EE-02
  label: oracle-power-allocation-audit
  method_family: RA-EE-MDP / RA-EE-MODQN
  status: offline-audit-only
  purpose: >
    Opt-in fixed-association trajectory replay for an offline oracle /
    heuristic power-allocation upper-bound proof. This is a new
    resource-allocation extension surface only. It is not old EE-MODQN, not
    Catfish, not multi-Catfish, not a HOBS optimizer, and not a frozen baseline
    replacement.

resolved_assumptions:
  hobs_power_surface:
    assumption_id: ASSUME-MODQN-REP-026
    value:
      mode: phase-03c-b-power-codebook
      inactive_beam_policy: zero-w
      power_codebook_profile: fixed-mid
      power_codebook_levels_w: [0.5, 0.75, 1.0, 1.5, 2.0]
      total_power_budget_w: 8.0
      active_base_power_w: 0.25
      load_scale_power_w: 0.35
      load_exponent: 0.5
      max_power_w: 2.0
      units: linear-w
      provenance: >
        RA-EE-02 finite-codebook audit surface. It is used only to test
        whether fixed association trajectories admit budget-respecting power
        allocations that improve system EE under QoS guardrails.
  ra_ee_02_oracle_power_allocation_audit:
    assumption_id: ASSUME-MODQN-REP-026
    value:
      artifact_namespace: artifacts/ra-ee-02-oracle-power-allocation-audit
      method_label: RA-EE-MDP / RA-EE-MODQN
      fixed_association_trajectories:
        - hold-current
        - random-valid
        - spread-valid
        - phase03c-c-candidate-best-eval-if-available
      power_candidates:
        - fixed-control
        - load-concave
        - budget-trim
        - qos-tail-boost
        - constrained-oracle
      fixed_control_power_w: 1.0
      codebook_levels_w: [0.5, 0.75, 1.0, 1.5, 2.0]
      total_power_budget_w: 8.0
      per_beam_max_power_w: 2.0
      inactive_beam_policy: zero-w
      p05_throughput_min_ratio_vs_control: 0.95
      served_ratio_min_delta_vs_control: 0.0
      outage_ratio_max_delta_vs_control: 0.0
      oracle_max_demoted_beams: 3
      training: not-run
      catfish: disabled
      multi_catfish: disabled
      frozen_baseline_mutation: forbidden
      claim_boundary: >
        Offline upper-bound proof only. A PASS authorizes RA-EE-03 design work,
        not RL training, not Catfish, not old EE-MODQN promotion, and not any
        HOBS optimizer claim.
