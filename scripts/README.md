# Script Entry Points

Current scripts are scaffolds that reserve the intended command surface:

1. `train_modqn.py`
   Future: default-scenario training run.
2. `run_sweeps.py`
   Future: Figure/Table sweeps for users, satellites, and speeds.
3. `export_ntn_sim_core_bundle.py`
   Future: export one stable run bundle for `ntn-sim-core` consumption.
