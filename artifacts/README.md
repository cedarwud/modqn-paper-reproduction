# Artifacts

This directory is reserved for exported run bundles, plots, and replay/timeline outputs.

Do not treat generated artifacts as source-of-truth configuration. The authoritative inputs remain:

1. the repo-local paper snapshot under `paper-source/`
2. the SDDs in `docs/phases/`
3. the assumption register
