"""Test package marker for repo-local helper imports.

Phase 04A's semantic golden suite imports a small helper package under
``tests/refactor_golden``. Marking ``tests/`` as a package keeps those
imports explicit without changing runtime code under ``src/``.
"""
